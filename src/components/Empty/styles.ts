import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        borderTopColor: "#333333",
        paddingVertical: 48,
        borderTopWidth: 1,
        borderStyle: "solid",
    },
    image: {
        marginBottom: 16,
    },
    textOne: {
        color: "#808080",
        fontWeight: "bold",
    },
    textTwo: {
        color: "#808080",
    }
})